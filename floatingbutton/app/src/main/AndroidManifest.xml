<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.welcome.floatingbutton">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/post_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".servicedisplay" />
        <activity android:name=".removalActivity" />
        <activity android:name=".Inventory" />
        <activity android:name=".disposeActivity" />
        <activity android:name=".Updatebarcode" />
        <activity android:name=".Updatedbarcode" />
        <activity android:name=".divert" />
        <activity android:name=".divert2" />
        <activity android:name=".installActivity" />
        <activity android:name=".install2"></activity>
    </application>

</manifest>